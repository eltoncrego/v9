import styled from "styled-components";
import { spacing } from "../../shared/app-styles";

export const StyledResumeSection = styled.section`
  margin-bottom: ${spacing[5]};
`;

export const StyledJobTitle = styled.h3`
  margin: 0;
`;

export const StyledTimeSpanCaption = styled.h4`
  margin: 0;
  font-weight: normal;
`

export const StyledCompanyInformation = styled.h5`
  margin-top: ${spacing[2]};
  font-weight: normal;
`